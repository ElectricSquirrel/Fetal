<resources xmlns:android="http://schemas.android.com/apk/res/android">

    <!--
        Base application theme, dependent on API level. This theme is replaced
        by AppBaseTheme from res/values-vXX/styles.xml on newer devices.
    -->
    <style name="AppBaseTheme" parent="android:Theme.Light">
        <!--
            Theme customizations available in newer API levels can go in
            res/values-vXX/styles.xml, while customizations related to
            backward-compatibility can go here.
        -->
    </style>

    <!-- Application theme. -->
    <style name="AppTheme" parent="AppBaseTheme">
        <!-- All customizations that are NOT specific to a particular API-level can go here. -->
    </style>
    
    <!-- Common -->
    <style name="auto_width_height">
        <item name="android:layout_width">wrap_content</item>
        <item name="android:layout_height">wrap_content</item>
    </style>
    
    <style name="match_width_height">
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">match_parent</item>
    </style>
    
    <style name="match_width_auto_height">
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">wrap_content</item>
    </style>
    
    <style name="auto_width_match_height">
        <item name="android:layout_width">wrap_content</item>
        <item name="android:layout_height">match_parent</item>
    </style>
    
    <style name="hidden">
        <item name="android:visibility">gone</item>
    </style>
    
    <style name="top" parent="match_width_auto_height">
        <item name="android:orientation">horizontal</item>
        <item name="android:background">#fe90a9</item>
        <item name="android:padding">10dp</item>
        <item name="android:layout_alignParentTop">true</item>
    </style>
    
    <style name="bottom" parent="match_width_auto_height">
        <item name="android:orientation">horizontal</item>
        <item name="android:background">#fe90a9</item>
        <item name="android:padding">10dp</item>
        <item name="android:layout_alignParentBottom">true</item>
    </style>
    
    <style name="bottom_item" parent="auto_width_height">
        <item name="android:layout_weight">1</item>
    </style>

</resources>
